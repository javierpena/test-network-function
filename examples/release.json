{
  "description": "Check that the container image is running RHEL 8",
  "testResult": 0,
  "testTimeout": 10000000000,
  "reelFirstStep": {
    "execute": "grep '^Red Hat' /etc/redhat-release\n",
    "expect": [
      "(?m)Red Hat Enterprise Linux release (\\d+)\\.(\\d+).*$"
    ],
    "timeout": 10000000000
  },
  "identifier": {
    "url": "http://test-network-function.com/tests/jpenaRedHatRelease",
    "version": "v1.0.0"
  },
  "resultContexts": [
    {
      "pattern": "(?m)Red Hat Enterprise Linux release (\\d+)\\.(\\d+).*$",
      "defaultResult": 1,
      "composedAssertions": [
        {
          "assertions": [
            {
              "groupIdx": 1,
              "condition": {
                "type": "intComparison",
                "input": 8,
                "comparison": "=="
              }
            }
          ],
          "logic": {
            "type": "and"
          }
        }
      ]
    }
  ]
}
